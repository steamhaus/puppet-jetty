---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data
hierarchy:
  - name: "OS release"
    path: "os/%{facts.os.name}-%{facts.os.release.major}"
    
  - name: "common"
    path: "common.yaml"